document.addEventListener('change', (e) => {
  const requestSize = e.target.closest?.('[data-request-page-size]');
  if(requestSize){
    REQUEST_PAGE_SIZE = normalizeTablePageSize(requestSize.value, REQUEST_PAGE_SIZE);
    localStorage.setItem('requestPageSize', String(REQUEST_PAGE_SIZE));
    resetRequestPaging();
    if(snapshot?.ok && workspaceMode === 'analytics'){
      setPagedTableLoading?.('requests', true, 0);
      refreshRequestPageCache(0, { force: true }).then(async () => {
        await ensureRequestPageInBoundsAfterLoad?.();
        scrollPagedTableToTop?.('requests');
        if(snapshot?.ok && workspaceMode === 'analytics') patchAnalyticsSlotsForState(snapshot, { tableOnly: true });
      }).catch(() => setPagedTableLoading?.('requests', false, 0))
        .finally(() => clearPagedTableLoading?.('requests'));
      patchAnalyticsSlotsForState(snapshot, { tableOnly: true });
    }
    e.stopImmediatePropagation?.();
    return;
  }
  const sessionSize = e.target.closest?.('[data-session-page-size]');
  if(sessionSize){
    SESSION_PAGE_SIZE = normalizeTablePageSize(sessionSize.value, SESSION_PAGE_SIZE);
    localStorage.setItem('sessionPageSize', String(SESSION_PAGE_SIZE));
    resetSessionPaging();
    if(snapshot?.ok && workspaceMode === 'sessions'){
      setPagedTableLoading?.('sessions', true, 0);
      refreshSessionPageCache(0, { force: true }).then(async () => {
        await ensureSessionPageInBoundsAfterLoad?.();
        scrollPagedTableToTop?.('sessions');
        if(snapshot?.ok && workspaceMode === 'sessions') patchSessionView(snapshot, { table: true, toolbar: false, inspector: true, pageChange: true });
      }).catch(() => setPagedTableLoading?.('sessions', false, 0))
        .finally(() => clearPagedTableLoading?.('sessions'));
      patchSessionView(snapshot, { table: true, toolbar: false, inspector: true, pageChange: true });
    }
    e.stopImmediatePropagation?.();
  }
});
document.addEventListener('change', (e) => { if(handleDateRangeDraftInput(e.target)) return; const tags = e.target.closest('[data-session-tags]'); if(tags){ const key = tags.dataset.sessionTags; sessionMeta[key] = { ...(sessionMeta[key] || {}), tags: normalizeTags(tags.value) }; saveSessionMeta(); setRefreshState(TXT.savedLocal); clearTimeout(lastToastTimer); lastToastTimer = setTimeout(() => setRefreshState(''), 900); if(workspaceMode === 'sessions'){ patchSessionRow(key); patchSessionOverview(snapshot); patchSessionToolbar(snapshot); return; } if(snapshot?.ok) render(snapshot, { windowLayout: false, instantChart: true, partial: true }); return; } const sel = e.target.closest('[data-select]'); if(!sel) return; if(sel.dataset.select === 'source'){ switchSourceFilter(sel.value, { chartDelayMs: 40 }).catch(() => { if(snapshot?.ok) render(snapshot, { windowLayout: false, instantChart: true, deferHeavy: true, partial: true }); }); return; } if(sel.dataset.select === 'model'){ resetIncrementalRenderLimits('all'); resetRequestPaging(); modelFilter = sel.value; beginDashboardRequestGeneration(); localStorage.setItem('statsModel', modelFilter); if(snapshot?.ok && patchAnalyticsSlotsForState(snapshot, { deferHeavy: true })) return; if(snapshot?.ok) render(snapshot, { windowLayout: false, instantChart: true, deferHeavy: true, partial: true }); } if(sel.dataset.select === 'project'){ resetIncrementalRenderLimits('all'); resetRequestPaging(); analyticsProjectFilter = sel.value || 'all'; beginDashboardRequestGeneration(); localStorage.setItem('statsProject', analyticsProjectFilter); if(snapshot?.ok){ patchAnalyticsAfterRequestPageRefresh({ deferHeavy: true, chartDelayMs: 80 }).catch(() => render(snapshot, { windowLayout: false, instantChart: true, deferHeavy: true, partial: true })); } return; } if(sel.dataset.select === 'refresh'){ refreshEvery = sel.value; localStorage.setItem('statsRefreshEvery', refreshEvery); setupAutoRefresh(); } if(sel.dataset.select === 'range'){ resetIncrementalRenderLimits('all'); resetRequestPaging(); resetSessionPaging(); rangeFilter = normalizeRangeFilter(sel.value); beginDashboardRequestGeneration(); const days = Number(String(rangeFilter).replace('d', '')); if(Number.isFinite(days)) localStorage.setItem('customRangeDays', String(days)); localStorage.setItem('statsRange', rangeFilter); if(snapshot?.ok && workspaceMode === 'analytics' && patchAnalyticsSlotsForState(snapshot, { deferHeavy: true })) return; if(snapshot?.ok && workspaceMode === 'sessions' && patchSessionView(snapshot, { table: true, toolbar: false, inspector: true })) return; if(snapshot?.ok) render(snapshot, { windowLayout: false, instantChart: true, deferHeavy: true, partial: true }); } if(sel.dataset.select === 'sessionSort'){ resetIncrementalRenderLimits('sessions'); resetSessionPaging(); sessionSort = sel.value; localStorage.setItem('sessionSort', sessionSort); patchSessionsOrRender({ table: true, toolbar: true, inspector: true, overview: true }); } if(sel.dataset.select === 'sessionTag'){ resetIncrementalRenderLimits('sessions'); resetSessionPaging(); sessionTagFilter = sel.value; localStorage.setItem('sessionTagFilter', sessionTagFilter); patchSessionsOrRender({ table: true, toolbar: true, inspector: true, overview: true }); } if(sel.dataset.select === 'sessionProject'){ resetIncrementalRenderLimits('sessions'); resetSessionPaging(); sessionProjectFilter = sel.value; beginDashboardRequestGeneration({ preserveBoundary: true }); localStorage.setItem('sessionProjectFilter', sessionProjectFilter); patchSessionsOrRender({ table: true, toolbar: true, inspector: true, overview: true }); } });
document.addEventListener('input', (e) => { if(handleDateRangeDraftInput(e.target)) return; const bulkTags = e.target.closest('[data-bulk-meta-tags]'); if(bulkTags){ bulkMetaTagsDraft = bulkTags.value; return; } const bulkNote = e.target.closest('[data-bulk-meta-note]'); if(bulkNote){ bulkMetaNoteDraft = bulkNote.value; return; } const savedViewName = e.target.closest('[data-saved-session-name]'); if(savedViewName){ savedSessionViewNameDraft = savedViewName.value; return; } const note = e.target.closest('[data-session-note]'); if(note){ const key = note.dataset.sessionNote; sessionMeta[key] = { ...(sessionMeta[key] || {}), note: note.value }; saveSessionMeta(); setRefreshState(TXT.savedLocal); clearTimeout(lastToastTimer); lastToastTimer = setTimeout(() => setRefreshState(''), 800); return; } const rename = e.target.closest('[data-rename-input]'); if(rename){ renameDraft = rename.value; return; } const q = e.target.closest('[data-query]'); if(!q) return; const scope = q.dataset.query === 'sessions' ? 'sessions' : 'analytics'; if(scope === 'sessions'){ sessionQuery = q.value; localStorage.setItem('statsSessionQuery', sessionQuery); } else { analyticsQuery = q.value; localStorage.setItem('statsAnalyticsQuery', analyticsQuery); } beginDashboardRequestGeneration({ preserveBoundary: true }); const app = document.getElementById('app'); app?.classList.add('is-typing'); clearTimeout(queryRenderTimer); queryRenderTimer = setTimeout(() => { queryRenderTimer = null; resetIncrementalRenderLimits(scope === 'sessions' ? 'sessions' : 'requests'); let patched = false; if(scope === 'sessions'){ resetSessionPaging(); patched = Boolean(snapshot?.ok && workspaceMode === 'sessions' && patchSessionView(snapshot, { table: true, toolbar: false, inspector: true, overview: false })); } else { resetRequestPaging(); patched = Boolean(snapshot?.ok && workspaceMode === 'analytics' && patchAnalyticsSlotsForState(snapshot, { lowerOnly: true })); } if(!patched && snapshot?.ok) render(snapshot, { windowLayout: false, instantChart: true, partial: true }); requestAnimationFrame(() => { const next = document.querySelector(`[data-query="${scope}"]`); if(next){ next.focus(); next.setSelectionRange(next.value.length, next.value.length); } app?.classList.remove('is-typing'); }); }, 140); });
