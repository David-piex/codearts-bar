# CodeArts Bar 1.16.37

发布日期：2026-07-19

`1.16.37` 是面向 GitHub 主仓库发布的稳定版本，延续 `1.16.36` 的跨端会话管理能力，并集中收敛聚合性能、发布可靠性和开发者工作台视觉一致性。

## 修复与改进

- Desktop dashboard 使用 lean 聚合路径，减少首屏未消费的扩展性能查询；rollup 全命中时可在打开 SQLite 前直接返回。
- 模型筛选的会话汇总通过完整 token sidecar 限定 session ID，再与 session sidecar 合并，保持模型和时间范围语义。
- 聚合缓存、rollup 恢复、SQL.js/native 热路径和真实 fixture 测试增加回归覆盖。
- 发布目录原子替换继续对 `EPERM`、`EBUSY` 和 `EACCES` 做限时重试，降低 Windows 文件索引或杀毒扫描造成的失败。
- Desktop、VS Code 和 JetBrains 工作台统一冷灰画布、电蓝交互强调、焦点环、placeholder 和 disabled 对比度，并更新视觉基线。
- Desktop 在桌面端与 CLI 数据源之间切换时保留现有表格和图表节点，使用轻量忙碌状态更新内容，并抑制局部 patch 的入场动画，消除切换闪烁。
- Desktop 为 dashboard 请求和 render 后置任务增加代际校验；新的来源、筛选或实时快照到达时会取消过期 timer、RAF、idle callback 与延迟分析任务，旧结果不再覆盖当前界面或滚动位置。
- 图表固定点改用静态高亮，移除持续运行的 `requestAnimationFrame` 重绘循环，降低 dashboard 长时间打开时的 CPU 占用。
- 会话分页大小切换期间保留已显示内容，新数据就绪后再局部替换，避免表格短暂清空和布局跳动。
- Desktop 批量归档与恢复收敛为单次 IPC；同一数据库的写入在一个 SQLite 事务内完成，失败时整批回滚、恢复选择并刷新真实状态，每批最多处理 `500` 个会话。
- GitHub 作为主仓库，README、npm repository 元数据、Release 和 clone 指引统一指向 GitHub；Gitee 保留为同步镜像。

## 本地验证

- 重新构建 Windows 安装版、便携版、VSIX、JetBrains 插件和 CLI/npm 包。
- 完整测试、Electron E2E、VS Code E2E、dashboard/分页/聚合压力测试、视觉回归、产物体积门禁和发布产物 smoke 全部通过。
- JetBrains 插件已通过 IDEA 2024.2、2024.3、2025.1、2025.2、2025.3.6、2026.1.4 和 2026.2 Plugin Verifier，发布范围为 build `242` 至 `262.*`。
- npm 安全审计结果为 `0 vulnerabilities`；renderer bundle、覆盖率和热路径性能均在项目质量预算内。
- 发布产物通过 `SHA256SUMS.txt` 校验，版本元数据统一为 `1.16.37`。

## 发布产物

- `CodeArts-Bar-Setup-1.16.37-x64.exe`
- `CodeArts-Bar-Portable-1.16.37-x64.exe`
- `codearts-bar-status.vsix`
- `codearts-bar-jetbrains-1.16.37.zip`
- `codearts-bar-cli.zip`
- `codearts-bar-cli-standalone.zip`
- `codearts-bar-1.16.37.tgz`
- `SHA256SUMS.txt`
